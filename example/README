examples of huh v2
x = save output of execution of variable to another variable
s = set variable to given value
m = merge two variables into one
f = flush all variables (unset)
u = unset variable
r = redirect command to location
a = append command to location
p = pipe command defined by variable to command defined by variable and save output to variable defined by first agr
g = get content of variable and print them
i = if variable = variable do "command from variable" else "command from variable" 
j = add another line (+ newline) to normal variable
w = read from input and save as variable

execution:
shebang should be "#!/usr/bin/env huh" 
you can also $ huh $file
